Show historical SEO memory for Locum Career Hub.

## Steps

1. Run `.\seo-memory\run.ps1 history --limit 40` (add `--url` / `--keyword` if the user named one).
2. Also run `.\seo-memory\run.ps1 context`.
3. Summarize: recent audits, shipped changes, open vs completed recommendations, experiments (winners/losers).
4. Do not invent history — only cite memory + optional live GSC if needed for “current vs then”.
